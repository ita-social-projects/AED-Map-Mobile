import mapPin from '../../../../content/images/pin-icon.jpg';

const SYMBOL_LAYOUT = {
  iconImage: mapPin,
  iconSize: 0.05,
  iconOffset: [0, -520],
  textField: ['get', 'title'],
  textFont: ['Open Sans Semibold', 'Arial Unicode MS Bold'],
  textSize: 16,
  textOffset: [0, 0.2],
  textAnchor: 'top'
};

export default SYMBOL_LAYOUT;
